const  {describe, it} = require('mocha'),
  {expect} = require('chai'),
  helpers = require('../../src/js/infotable');
  
  
 describe('prepareCalendar', ()=>{
	 it('should return full calendar based on From and To dates', ()=>{
		const infotable = new Infotable();
		infoTable.TableExport = ()=>{}
	 });
 }); 